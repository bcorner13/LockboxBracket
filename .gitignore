# FreeCAD backups
*.FCBak

# Session-in-progress snapshots (timestamped working state, not committed)
*.Session-*.FCStd

# Slicer output — regenerable, kept only for cost analysis
gcode/*
!gcode/.gitkeep

# macOS
.DS_Store

# Python
__pycache__/

# Claude Code — ignore personal local state, keep the committed guard hook
.claude/settings.local.json
